services:
  telegram_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rmon_telegram_bot
    restart: unless-stopped
    command: python scripts/run_bot.py
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app/src:/app
      - WHISPER_MODEL=medium
      - WHISPER_DEVICE=cpu
      - WHISPER_COMPUTE=int8
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./configs:/app/configs:ro
    deploy:
      resources:
        limits:
          cpus: '8.0'
          memory: 16G

  market_monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rmon_market_monitor
    restart: unless-stopped
    command: python scripts/run_monitor.py --query "авточехлы" --limit 25
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app/src:/app
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./configs:/app/configs:ro
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8G